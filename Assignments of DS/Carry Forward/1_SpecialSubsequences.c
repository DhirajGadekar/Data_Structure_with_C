#include<stdio.h>

int specialSubsequence(char *str) {
	int size = sizeof(str)/sizeof(char);

	for(int i = 0; i < size; i++) {
		
		for(int j = i + 1; ) {
		}
	}
}
void main() {
	
	char *str = "ABCGAG";
}
